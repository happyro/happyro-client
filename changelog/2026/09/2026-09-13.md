# 2026-09-13

- 地图图鉴与地图搜索采用与后台一致的版本化地图目录，统一名称、支持范围和分线筛选；生成来源为根仓库 tools/generate-map-catalog.mjs。

- 技能树标题、查看技能信息文字及右侧按钮上移 2px，checkbox 保持原位。

- Admin 不可用时隐藏物品图鉴，职业转换成功后关闭冒险工具；左上角菜单交换冒险工具与签到的位置。

- 聊天栏增加滚动固定与窗口位置锁定；消息记录设置增高、修正悬停边框，并实现当前 Tab 全部开启及异步图标状态同步。

- 登录页“记住账号”与注册按钮同排并留出密码框间距；注册按钮加宽至 73px，对齐密码框可见右边缘。

- 冒险工具在后台不可用或能力查询失败时隐藏依赖 Admin 的页签，并自动回退到可用页签；地图、NPC、魔物等本地功能继续保留。

- Alt+A 属性窗口保留原皮肤与行高，横向扩展数值区以容纳 32767；同步调整背景分格、基础属性、加成、加点消耗、箭头和战斗数值的位置。完整 PWA 已更新，并截图核对基础值与加成分格。

- 修复背包卡片详情“查看”按钮中文换行溢出，增加按钮可用宽度并保持文字单行居中；按钮距详情容器底部 6px。

- 世界地图切换魔物显示及加载资料后重新排布标签，并按标签实际边缘更新连接线；隐藏的重叠楼层不再拦截鼠标，保留完整楼层名称。
- 从当前地图入口重新打开导航时刷新当前地图；未选坐标时随机传送导航展示的地图，按钮状态与发送请求使用同一目标。完整 PWA 已更新本机验收入口。

- 地图传送后恢复原先打开的冒险工具窗口，保留地图图鉴选点和路线状态；仅在传送前记录打开状态，使用独立恢复标志避免函数同名赋值异常打断进入地图。完整 PWA 已更新，按要求未运行测试。

- 导航缺少地图图片时绘制地形底图，底图缓存与寻路线程数据分离；修复传送入口行走、对话传送衔接、切图后续行及路线拼接/分段显示。
- 导航、地图图鉴与右上角小地图统一使用蓝色角色三角箭头，目标使用紫色标记；当前地图取消自动选点，未选点时可随机传送。
- 完整 PWA 已构建并更新本机入口；按要求未运行测试。

- 进入地图时默认展开聊天输入栏，并同步聊天模式按钮状态。

- 更新 NPC 对话按钮测试以匹配现有 `is-visible` class 行为，恢复客户端全量测试对当前实现的有效校验。

- 技能说明使用固定标题栏拖动和独立滚动正文，限制窗口高度并在打开、拖动结束及视口变化后约束边界，避免长说明超出画面。

- 移除技能说明时间标签中的“（基础）”，其他说明与数值保持不变。

- 技能说明区分基础独立冷却、施放后延迟、可变/固定吟唱及移动延迟；主动技能无独立冷却时明确标注，缺少服务端资料时标注未提供，被动技能不增加冷却栏。

- 技能冷却按当前连接中的技能 ID 独立保存，修复冷却包早于快捷栏时丢失显示；快捷栏、技能窗口和施放判断共享剩余时间，支持服务器缩短或清除冷却。
- 修正被动技能被错误视为自身主动技能的问题；冷却未结束时显示明确剩余秒数。
- 修正状态汉化的 Renewal 负重阈值为 70%，保留无计时状态标记；技能描述的持续时间和冷却按等级标注，避免去重丢失等级关系。

- 修复角色服 AID 握手分段、typed-array 偏移及连接切换时旧数据混入新连接的问题。
- 未知包边界、非法变长长度和 NPC 响应计数明确报错；不完整包有绝对等待期限。
- 断线清理 NPC 查询与传送请求；请求先登记再发送，处理发送失败、超时及迟到响应。
- 导航 NPC 可用性查询复用公共请求服务，按每批最多 50 条拆分并合并响应。
- 新增真实编码字节、所有分段位置、合包、重连与超时回归测试。
