# 2026-09-23

## Fixed

- 同步 main 的连接生命周期修复：WebSocket 连接结果只回调一次，主动关闭旧登录连接后的延迟错误和关闭事件不再导致选角界面误报“服务器连接失败”；保留真实断线通知，并正确处理握手失败。

- 手机登录、选角和创建角色支持横竖屏布局及原生滚动，软键盘弹出后调整可视区域，旋转时保留输入和选择；复用公共角色业务与 main 的移动端组件。
- 横屏要求仅在进入游戏及游戏中生效：竖屏进入时等待旋转并可取消，游戏中竖屏显示遮罩且保持连接，返回登录或选角后解除；设备方向判断不受软键盘影响。
- 移除手机登录页的退出按钮；注册提示统一使用登录页的浅色卡片和暖色按钮，支持横竖屏滚动、焦点管理及独立注册页面确认。
- 登录和注册提示按钮的悬停样式仅对精确鼠标指针启用，避免触屏点击注册后持续显示选中态；保留按下反馈及键盘焦点提示。
- 移动端登录不再加载桌面登录背景和旧画布进度条，资源与地图加载改用浅色专属进度页，连接服务器改用移动端等待动画；同步调整启动占位样式，并在连接失败时先清除等待页。
- 选角、创建角色及横屏提示统一为浅色；移动端锁定外层页面滚动和缩放，输入失焦后清除键盘偏移，保留内部原生滚动和游戏手势，避免页面错位后必须刷新。
- 修正移动端启动加载页内边距导致的居中偏移，适当放大选角人物预览，并将右上角返回改为有背景和边框的按钮。
- 恢复选角卡片原有尺寸，仅视觉放大人物预览，不再撑高角色边框或移动名称。
- 移除移动端选角的预约删除、取消预约和确认删除入口并禁用删除操作，角色删除仅在桌面端提供。
- 移动端竖屏选角属性最后一行统一去除底部分隔线，下方属性面板向上扩展 4px，底部操作区位置保持不变。
- 竖屏选角改为实际给下方属性面板增加 4px 布局高度，同时缩减上方列表，替换负外边距调整。
- 竖屏选角下方属性面板再增高 4px，累计增加 8px，上方角色列表相应缩减。
- 移动端选角属性面板增加顶部分隔线，空角色卡使用加号与创建提示；统一创建页返回按钮及种族卡片高度，发型和发色改为随种族、性别与外观同步的真实精灵预览。
- 移动端改用桌面独立发型图标及纯色色块，移除选项人物预览；属性顶部分隔线贴合列表底部边界，空角色提示按角色槽数据同步，避免已有人物仍显示创建提示。
- 恢复移动端选角属性面板顶部分隔线上方原有的 12px 留白。
- 统一竖屏选角最后一行属性与下方分隔线的间距，底线归属属性区域，移除属性与操作区之间的额外空隙。
- 选角返回登录的确认框改用移动端浅色原生模态框，支持确认、取消、焦点限制和横竖屏布局，并隔离底层选角快捷键。
- 在公共 UIManager 统一移动端消息、确认和错误弹窗，密码错误等提示自动使用浅色模态框；确认按钮统一为“确定”，初始焦点置于标题，重新登录时默认选中首个角色位。
- 公共移动端弹窗隔离全局游戏触摸事件，修复密码错误提示点击确定无效；页面清理时同时移除移动端弹窗。

- 增加默认隐藏的右侧中部前端日志侧栏，通过 URL debug=1 开关启用，在应用启动前捕获错误、控制台、HTTP 资源、WebSocket 元数据、界面及视口变化，每 5 秒记录状态；支持复制、下载及手动快照，隐藏输入值，不记录请求或封包正文，不改变连接时序。

- 修复局域网 HTTP 下日志按钮仅选中文本而未复制的问题，剪贴板 API 不可用时在点击事件内同步复制全文，并反馈复制结果。

- 修复角色预览头饰重影和相同外观角色闪烁：2D 精灵缓存按目标画布与精灵帧隔离，外观不变时复用，换色时生成新图而不覆盖旧图；移动端选角人物下移 8px，缩小与名称的间距。

- 新增独立移动端横屏游戏主界面骨架：实时人物与状态信息、地图网格及位置、菜单和聊天摘要／输入；预留摇杆、攻击与技能槽位，尚未接入操作。隔离界面事件并清理换图、退出及断线生命周期，保留桌面组件装配与实体手柄支持。

- 接入移动端固定摇杆、独立双指攻击、点选目标与交谈／拾取／传送入口，提供镜头调整按钮；复用现有战斗校验并处理触点取消、换图与迟到攻击响应，保留桌面交互和实体手柄路径。技能槽位仍为占位。

- 接入移动快捷槽位真实图标、等级／数量、冷却与可用状态，支持三槽翻页、点选配置／替换／清空及服务端保存；复用物品和技能业务，支持自身、目标与地面施法及取消，保持摇杆触点独立和桌面默认行为。

- 接入移动端横屏背包分类、物品详情、点击使用、穿戴／卸下和确认设置快捷槽，复用服务端物品状态与业务；统一非堆叠装备数量判断，隔离面板触控并避免旧摇杆触点释放误关面板，保留桌面交互。

- 新增独立移动装备面板，按普通／时装／影子部位查看、卸下和从背包更换装备，支持左右部位选择及多部位占用展示；人物主要属性直接随服务器回包更新，按角色隔离，保留桌面属性和穿脱交互。

- 新增移动技能面板，支持技能分类、说明与前置条件、确认学习／升级、施放等级和快捷槽绑定；资格与技能点以服务器回复为准。
- NPC 对话、选项、下一步、文字／数字输入及买卖选择改用独立移动面板；按服务器协议区分菜单取消和最终关闭，避免空值误提交；肖像加载保留输入焦点，旧对话收尾不覆盖新打开的仓库。
- 普通、点数、限量及材料兑换 NPC 商店提供数量选择、订单核对与确认，显示货币、库存及材料要求，重新核验数量、金额与材料并阻止重复提交；背包增加显示排序和带数量的丢弃确认。
- 新增移动仓库／手推车面板，支持分类、容量／重量、点选目标容器与数量及六向转移；复用原封包业务，完整列表开始时清理移动端背包、装备及手推车旧数据，保留桌面窗口与交互路径。

- 新增移动任务列表、分类、目标进度、奖励与启用切换；任务地点可打开地图，支持世界区域、地图搜索、真实地图图片和当前地图位置查看。
- 聊天支持频道、私聊和消息筛选，移动私聊不再受桌面一对一弹窗偏好影响；新增好友、队伍、公会管理，支持职位权限／税率、公告、关系、公会技能学习与快捷绑定、徽章上传，操作前核对当前会话与权限。
- 传送、鉴定、自动技能选择、箭矢／料理／锻造、材料转换、修理、手推车外观和怪物识别接入独立移动面板，沿用原业务封包与取消协议。
- 新增移动卡片镶嵌、精炼、评级、随机／指定／升级／重置附魔、Laphine 合成／升级、装备改造和装备方案；确认前重新校验物品、材料与费用，附魔回包后结束会话并显示结果。

- 新增移动玩家交易面板：点选发起、数量与金额、双方锁定、再次确认成交及取消，按服务器回包处理背包预留与退回。
- 新增移动售卖／收购摊设置、订单核对、我的摊位与库存记录、关闭摊位，以及访问玩家摊位购买／出售；补齐 20211103 收购更新与成交删物品协议处理，保留桌面入口。
- 修复移动交易／开店面板在物品图标异步加载后重置数量和单价输入的问题。
- 移动页面外层改为禁止程序滚动的裁切区域，修复 WebKit 聚焦游戏输入框后整个 HUD 横向偏移；内部面板保持原生滚动。

- 移动银行加入金额上限、存取二次确认、等待回包防重复提交和服务端余额同步；修正银行查询封包 64 位余额的读取位置。
- 移动菜单加入独立画面／声音设置，支持草稿、保存、取消与恢复默认；复用现有偏好和实时渲染参数，不修改桌面窗口布局。

- 移动邮件使用独立横屏触控面板，支持分类搜索、分页、阅读、领取、删除确认、收件人校验、点选附件与发送核对；新邮件从菜单提示。补齐旧 NPC 邮箱入口的移动分流，保留桌面回调。
- 邮件请求保留完整 64 位邮件 ID，中文标题和正文按 UTF-8 字节计长，避免高位丢失与重复扩容。

- 移动宠物面板接入服务器状态、喂食、表演、改名、收回、饰品卸下和自动喂食设置；进化展示并重新核验亲密度与材料，保留二次确认和回包状态。捕捉改为点选可见魔物，孵化改为点选宠物蛋，桌面选择器和捕捉动画保持原分流。

- 修正生命体与佣兵技能施法者和射程来源，地面技能识别完整伴侣技能范围并使用对应实体移动；伴侣离场后的技能请求不再访问失效实体。

- 生命体／佣兵追近技能改由各自移动结束触发并重验生命周期，避免玩家移动误触发；快捷栏支持完整伴侣技能范围，移动端按实际施法者检查 SP、选择自身并处理伴侣更换。

- 新增移动生命体／佣兵独立信息面板，接入服务器状态、喂食／改名／解除／返回身边确认、攻击模式和生命体自动喂食及技能学习；移动回包不再挂载桌面信息窗口。
